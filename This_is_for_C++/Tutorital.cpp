#include <iostream> 
#include <string>
//this is the header, pre-processor derivatives. 
//iostream is the library

using namespace std; // this allows us to use functions within the library

int main(){//start of the program
    int //data type that adds integers, numbers
    bool // data type that adds a value either yes or no
    char // data type that adds characters alphabets
    float // data type that adds numbers with decimal or fraction
    long // this can be added to char and indicates that it is a long char
    double // this indicates a 

    string //this indicates a string represented inside a double qoutation mark

    append // this allows to concatenate two strings
    length // allows you to calculate the length of a string

    string variable1 = "Hello World";
        variable1[1] = 'i';
        cout << variable1;

    variable[]//the bracket suggests an amount of number that will indicate how long that interger is gonna be

    cout<< //this prints the output
    cin>>// this is for input but you have to store it in a variable

    <<endl //this allows you to create a new line same with; /n 

//conditions in c++
    if (condition){
        //code
    }
    else if (condition){//used in conditions instead elif in python
        //code
    }
    else (condition){
        //code
    }

} // end of the program.

